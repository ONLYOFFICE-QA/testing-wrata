<%= form_with(model: tested_server, local: true) do |form| %>
  <% if tested_server.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tested_server.errors.count, "error") %> prohibited this tested_server from being saved:</h2>

      <ul>
      <% tested_server.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :tested_server_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
